{% extends "layout.njk" %}

{% block content %}
<p>MINA BILDEEER</p>

<main class="container">
    <ul>
        <p>in the lisssst</p>
        {% for image in images %}
        <li>
            <h1><a href="/images/{{ image.id }}">{{ image.title }}</a></h1>
            <p>{{ image.year }}</p>
            <p>{{ image.description }}</p>
        </li>
        {% endfor %}
    </ul>
</main>
{% endblock %}